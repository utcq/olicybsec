# Flags shop

[> *Challenge (500)*](https://training.olicyber.it/challenges#challenge-43)

## Solution

The product cost is in the request body (mindblowing)

So we just need to POST to `/buy.php` with:
```json
id=2&costo=0
```

ID=2 is the flag product id
